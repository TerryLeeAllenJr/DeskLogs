'use strict';

describe('Service: deskLogs', function () {

  // load the service's module
  beforeEach(module('clientApp'));

  // instantiate service
  var deskLogs;
  beforeEach(inject(function (_deskLogs_) {
    deskLogs = _deskLogs_;
  }));
/*
  it('should do something', function () {
    expect(!!deskLogs).toBe(true);
  });
  */

});
