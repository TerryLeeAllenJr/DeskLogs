'use strict';

describe('Service: sounds', function () {

  // load the service's module
  beforeEach(module('clientApp'));

  // instantiate service
  var sounds;
  beforeEach(inject(function (_sounds_) {
    sounds = _sounds_;
  }));
/*
  it('should do something', function () {
    expect(!!sounds).toBe(true);
  });
  */

});
